# =============================================================================
# Zentrix.ai — Environment Variables (.env.example)
# Reference: ARCHITECTURE.md §11
# =============================================================================
# Copy this file to `.env` (or `apps/backend/.env`) and fill in real values.
# NEVER commit `.env` files containing real secrets to version control.

# ─── Environment & Application ──────────────────────────────────────────────
ENVIRONMENT=development
NODE_ENV=development
API_PORT=8000
CORS_ORIGINS=["http://localhost:3000"]

# ─── Application Database (PostgreSQL / Neon) ────────────────────────────────
# Connection string to the application Postgres/Neon instance (asyncpg driver)
APP_DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/zentrix

# ─── Authentication & Secrets ────────────────────────────────────────────────
# Secret key used for signing and verifying JWT authentication tokens
JWT_SECRET_KEY=change-this-to-a-secure-random-jwt-secret-key
JWT_ALGORITHM=HS256
JWT_EXPIRY_MINUTES=1440
# Development only: lets Connections and Monitoring run before auth UI is wired.
# Keep false in any shared or production environment.
DEV_CONNECTIONS_WITHOUT_AUTH=true

# Clerk server-side verification. Keep secret values backend-only.
CLERK_SECRET_KEY=
CLERK_ISSUER=
CLERK_JWKS_URL=
CLERK_PEM_PUBLIC_KEY=

# ─── Connection Credential Encryption ────────────────────────────────────────
# Fernet key used to encrypt customer database connection strings at rest
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
CONNECTION_ENCRYPTION_KEY=change-this-to-a-valid-fernet-key-e.g.-44-char-base64=

# ─── MLflow Tracking ─────────────────────────────────────────────────────────
# URI where models, metrics, and experiments are tracked
MLFLOW_TRACKING_URI=http://localhost:5000

# Promoted model artifacts. Keep these files outside git and mount them in deployment.
ANOMALY_MODEL_PATH=.artifacts/anomaly_model.joblib
TEMPORAL_MODEL_PATH=.artifacts/temporal_model.pt
RCA_MODEL_PATH=.artifacts/rca_model.joblib
FEATURE1_MANIFEST_PATH=.artifacts/manifest.json
DELTA_MODEL_PATH=.artifacts/delta_predictor.joblib
FORECASTING_MODEL_PATH=.artifacts/forecasting_model.joblib

# ─── Worker Configuration ────────────────────────────────────────────────────
# Polling frequency per connected customer database (seconds)
TELEMETRY_POLL_INTERVAL_SECONDS=60

# Canary observation duration before auto-commit / rollback evaluation (minutes)
CANARY_MONITOR_WINDOW_MINUTES=15

# Docker image used to clone customer databases for safe shadow experiments
SHADOW_DB_IMAGE=postgres:16-alpine

# ─── Shadow Pool (Model B) — real shadow databases for verification ─
# Superuser on the persistent shadow-pool Postgres. The backend uses
# these credentials to CREATE DATABASE shadow_<uuid> per experiment,
# pg_dump|pg_restore the customer schema into it, run paired workload
# replay, then DROP DATABASE on teardown.
SHADOW_POOL_HOST=shadow-pool
SHADOW_POOL_PORT=5432
SHADOW_POOL_USER=shadow_admin
SHADOW_POOL_PASSWORD=shadow_pool_dev_password
SHADOW_POOL_ADMIN_DB=shadow_admin
# Docker Compose overrides this to host.docker.internal; use 127.0.0.1 when the backend runs directly.
SHADOW_DB_HOST=127.0.0.1

# ─── Frontend Configuration ──────────────────────────────────────────────────
# Base URL the frontend calls for REST / SSE endpoints
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Frontend build-time authentication configuration.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/dashboard
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/dashboard

